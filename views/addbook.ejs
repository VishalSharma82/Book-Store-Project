<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sell Your Book</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <!-- Navbar -->
    <header class="bg-blue-600 text-white p-4 fixed w-full top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <a href="#" class="text-xl font-bold">📚 Booket</a>
            <input type="search" placeholder="Search Your's choice"
                class="hidden lg:block p-2 w-96 outline-none rounded-lg text-zinc-900">

            <!-- Desktop Navigation -->
            <nav class="hidden md:flex space-x-4">
                <a href="/" class="p-2 rounded-lg hover:bg-blue-800">Home</a>
                <a href="/about" class="p-2 rounded-lg hover:bg-blue-800">About</a>
                <a href="/booksbrowse" class="p-2 rounded-lg hover:bg-blue-800">Store</a>
                <a href="./#contact" class="p-2 rounded-lg hover:bg-blue-800">Contact</a>
                <a href="/addbook" class="p-2 rounded-lg hover:bg-blue-800">List Book</a>
                <a href="/account" class="bg-white text-blue-600 px-4 py-2 rounded-lg">My Account</a>
                <!-- <a href="#" class="bg-white text-blue-600 px-4 py-2 rounded-lg">Cart</a> -->
            </nav>

            <!-- Mobile Menu Button -->
            <div id="menu-icon" class="md:hidden text-2xl cursor-pointer">☰</div>
        </div>
    </header>

    <!-- Sidebar (Hidden by Default) -->
    <div id="sidebar" style="z-index: 999;"
        class="fixed top-0 left-0 w-64 h-full bg-blue-700 text-white flex flex-col space-y-4 p-5 transform -translate-x-full transition-transform duration-300">
        <button id="close-sidebar" class="text-xl self-end">✖</button>
        <a href="/" class="p-2 hover:bg-blue-800">Home</a>
        <a href="/about" class="p-2 hover:bg-blue-800">About</a>
        <a href="/booksbrowse" class="p-2 hover:bg-blue-800">Store</a>
        <a href="./#contact" class="p-2 hover:bg-blue-800">Contact</a>
        <a href="/addbook" class="p-2 hover:bg-blue-800">List Book</a>
        <a href="/account" class="bg-white text-blue-600 px-4 py-2 rounded-lg">My Account</a>
        <!-- <a href="#" class="bg-white text-blue-600 px-4 py-2 rounded-lg">Cart</a> -->
    </div>

    <!-- Overlay (Hidden by Default) -->
    <div id="overlay" class="fixed top-0 left-0 w-full h-full bg-black opacity-50 hidden"></div>

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-lg mt-32">
        <h2 class="text-2xl font-bold text-center mb-6">Add Your Book</h2>
        <form action="/addbook" method="POST" enctype="multipart/form-data" class="space-y-4">
            <div>
                <label class="block text-gray-700 font-medium">Ad Title</label>
                <input type="text" name="title" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
            </div>
        
            <div>
                <label class="block text-gray-700 font-medium">Add book for</label>
                <div class="flex items-center space-x-4">
                    <label><input type="radio" name="bookPurpose" value="sell" required> Sell</label>
                    <label><input type="radio" name="bookPurpose" value="exchange"> Exchange</label>
                    <label><input type="radio" name="bookPurpose" value="both"> Both</label>
                </div>
            </div>
            <div>
                <label class="block text-gray-700 font-medium">Book Type</label>
                <select name="bookType" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
                    <option value="">Please select book type</option>
                    <option value="new">New</option>
                    <option value="used">Used</option>
                </select>
            </div>
        
            <div id="bookConditionField" style="display: none;">
                <label class="block text-gray-700 font-medium">Book Condition (Used)</label>
                <div class="flex items-center space-x-4">
                    <label><input type="radio" name="bookCondition" value="excellent"> Excellent</label>
                    <label><input type="radio" name="bookCondition" value="good"> Good</label>
                    <label><input type="radio" name="bookCondition" value="fair"> Fair</label>
                </div>
            </div>
        
            <div>
                <label class="block text-gray-700 font-medium">Quantity Available</label>
                <input type="number" name="quantity" min="1" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
            </div>
        
            <!-- Pricing Details -->
            <div>
                <label class="block text-gray-700 font-medium">Your Price (₹)</label>
                <input type="number" name="price" min="0" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
            </div>
        
            <div>
                <label class="block text-gray-700 font-medium">Your Shipping Charges</label>
                <div class="flex items-center space-x-4">
                    <input type="number" name="shippingCharges" min="0" class="px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300">
                    <label class="flex items-center">
                        <input type="checkbox" name="freeShipping"> Free Shipping
                    </label>
                </div>
            </div>
        
            <!-- Seller Details -->
            <div>
                <h3 class="text-xl font-semibold mt-6">Your Details</h3>
                <div>
                    <label class="block text-gray-700 font-medium">Name</label>
                    <input type="text" name="sellerName" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-medium">Email Address</label>
                    <input type="email" name="sellerEmail" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-medium">Address</label>
                    <input type="text" name="sellerAddress" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-medium">Phone Number</label>
                    <input type="tel" name="sellerPhone" class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300" required>
                </div>
            </div>
        
            <div class="flex justify-center">
                <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600">Submit</button>
            </div>
        </form>
    </div>
</body>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const bookTypeSelect = document.querySelector("select[name='bookType']");
        const bookConditionField = document.getElementById("bookConditionField");
        const bookConditionInputs = document.querySelectorAll("input[name='bookCondition']");
    
        bookTypeSelect.addEventListener("change", function () {
            if (this.value === "used") {
                bookConditionField.style.display = "block";
                bookConditionInputs.forEach(input => input.setAttribute("required", "true"));
            } else {
                bookConditionField.style.display = "none";
                bookConditionInputs.forEach(input => input.removeAttribute("required"));
            }
        });
    });

    // Select elements
    const menuIcon = document.getElementById("menu-icon");
        const sidebar = document.getElementById("sidebar");
        const closeSidebar = document.getElementById("close-sidebar");
        const overlay = document.getElementById("overlay");

        // Open Sidebar
        menuIcon.addEventListener("click", () => {
            sidebar.classList.remove("-translate-x-full");
            overlay.classList.remove("hidden");
        });

        // Close Sidebar
        closeSidebar.addEventListener("click", () => {
            sidebar.classList.add("-translate-x-full");
            overlay.classList.add("hidden");
        });

        // Close Sidebar when clicking outside
        overlay.addEventListener("click", () => {
            sidebar.classList.add("-translate-x-full");
            overlay.classList.add("hidden");
        });
    </script>    
</html>
