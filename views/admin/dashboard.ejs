<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Dashboard | Booket</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">

  <!-- 🔵 TOP NAVBAR -->
  <nav class="bg-blue-600 px-6 py-4 flex items-center justify-between">
    <div class="text-white text-xl font-bold flex items-center gap-2">
      📘 Booket <span class="text-sm font-normal opacity-80">Admin</span>
    </div>

    <div class="flex gap-4">
      <a href="/admin/dashboard" class="text-white font-medium">Dashboard</a>
      <a href="/admin/manage-books" class="text-white font-medium">Manage Books</a>
      <a href="/admin/logout"
         class="bg-white text-blue-600 px-4 py-1 rounded-lg font-semibold">
        Logout
      </a>
    </div>
  </nav>

  <!-- 📊 DASHBOARD CONTENT -->
  <div class="max-w-7xl mx-auto px-6 py-10">

    <!-- HEADING -->
    <h2 class="text-3xl font-bold text-gray-800 mb-8">
      Admin Dashboard
    </h2>

    <!-- STAT CARDS -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">

      <!-- Total Books -->
      <div class="bg-gradient-to-r from-blue-500 to-blue-400 text-white p-6 rounded-xl shadow-lg">
        <p class="text-sm uppercase opacity-90">Total Books</p>
        <h3 class="text-4xl font-bold mt-2"><%= totalBooks %></h3>
      </div>

      <!-- Admin Books -->
      <div class="bg-gradient-to-r from-green-500 to-emerald-400 text-white p-6 rounded-xl shadow-lg">
        <p class="text-sm uppercase opacity-90">Admin Added Books</p>
        <h3 class="text-4xl font-bold mt-2"><%= adminBooks %></h3>
      </div>

      <!-- Users -->
      <div class="bg-gradient-to-r from-purple-500 to-pink-400 text-white p-6 rounded-xl shadow-lg">
        <p class="text-sm uppercase opacity-90">Total Users</p>
        <h3 class="text-4xl font-bold mt-2"><%= users %></h3>
      </div>
    </div>

    <!-- ACTION CARDS -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

      <!-- Add Book -->
      <div class="bg-white p-6 rounded-xl shadow hover:shadow-lg transition">
        <h3 class="text-xl font-semibold text-gray-800 mb-2">
          ➕ Add New Book
        </h3>
        <p class="text-gray-600 mb-4">
          Add brand new books manually or via Google Books API.
        </p>
        <a href="/admin/add-book"
           class="inline-block bg-blue-600 text-white px-5 py-2 rounded-lg hover:bg-blue-700">
          Add Book
        </a>
      </div>

      <!-- Manage Books -->
      <div class="bg-white p-6 rounded-xl shadow hover:shadow-lg transition">
        <h3 class="text-xl font-semibold text-gray-800 mb-2">
          📦 Manage Books
        </h3>
        <p class="text-gray-600 mb-4">
          Edit price, stock or remove books from the store.
        </p>
        <a href="/admin/manage-books"
           class="inline-block bg-green-600 text-white px-5 py-2 rounded-lg hover:bg-green-700">
          Manage Books
        </a>
      </div>

    </div>
  </div>

</body>
</html>
